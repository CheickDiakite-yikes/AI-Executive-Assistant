run = "npm run dev"

[ports]
  [ports.frontend]
    localPort = 5000
    externalPort = 80
    expose = true
  
  [ports.backend]
    localPort = 3001
    externalPort = 3001
    expose = true

[nix]
  channel = "stable-24_05"

[env]
  VITE_PORT = "5000"
  API_PORT = "3001"
